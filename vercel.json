{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "npm install --no-package-lock --ignore-scripts && npm --prefix ui ci",
  "buildCommand": "npm --prefix ui run build",
  "outputDirectory": "ui/.next"
}
